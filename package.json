{
  "scripts": {
    "build-css": "node-sass --include-path scss style/style.scss dist/style/style.css",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp details.html dist/details.html && cp 404.html dist/404.html && cp jobCards.js dist/jobCards.js && cp data.json dist/data.json && cp teamMembers.json dist/teamMembers.json && cp details.js dist/details.js && cp teams.js dist/teams.js && cp script.js dist/script.js && cp carousel.js dist/carousel.js && cp -r assets dist/assets && npm run build-css"
  },
  "dependencies": {
    "braces": "^3.0.3",
    "node-sass": "^9.0.0"
  },
  "devDependencies": {
    "stylelint": "^16.4.0",
    "stylelint-prettier": "^5.0.0",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
